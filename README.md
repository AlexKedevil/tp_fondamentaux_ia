# Les fondamentaux de l’IA — Rendus de TPs

Ce dépôt regroupe mes travaux pratiques du cours **« Les fondamentaux de l’IA »**.

## TP1 — Analyse d’un algorithme en fonctionnement (Iris)

- **Notebook** : `TP1/notebook/TP1_Iris_Pipeline_SHAP.ipynb`
- **Réponses (texte)** : `TP1/docs/answers.txt`
- **Sorties (images)** : `TP1/outputs/` (générées automatiquement par le notebook)
- **Sujet** : `TP1/docs/TP1.pdf`

### Exécution (recommandé)

```bash
cd TP1
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
jupyter lab
```

Puis ouvrir le notebook et faire **Run All**. Les figures seront sauvegardées dans `TP1/outputs/`.

## TP2 — Mini-projet IA (Cas A : Churn clients)

- **Notebook** : `TP2/notebook/TP2_CasA_Churn.ipynb`
- **Sorties (images)** : `TP2/assets/`
- **Sujet** : `TP2/docs/TP2.pdf`

### Exécution (recommandé)

```bash
cd TP2
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
jupyter lab
```

Puis ouvrir le notebook et faire **Run All**. Les figures seront sauvegardées dans `TP2/assets/`.

